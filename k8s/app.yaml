apiVersion: apps/v1
kind: Deployment
metadata:
  name: gestion-cotisations
  namespace: cotisations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gestion-cotisations
  template:
    metadata:
      labels:
        app: gestion-cotisations
    spec:
      automountServiceAccountToken: false
      containers:
        - name: gestion-cotisations
          image: biboudiagne/gestion-cotisations:acfec976f1ba0892fe80c9a68c12aa2194b41e91
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
          securityContext:
            capabilities:
              drop:
                - ALL
          readinessProbe:
            httpGet:
              path: /login
              port: 8080
            initialDelaySeconds: 45
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /login
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: gestion-cotisations
  namespace: cotisations
spec:
  selector:
    app: gestion-cotisations
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP